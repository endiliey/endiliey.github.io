# Travis CI for https://travis-ci.com/endiliey/endiliey.github.io/

language: node_js
node_js:
  - lts/*

script:
  - yarn run build

deploy:
  provider: pages
  github_token: $GH_TOKEN
  skip_cleanup: true
  local-dir: build
  keep-history: true
  on:
    branch: source